# C++
[source](../cpp/kernel.cpp)

This one is the reference code that i wrote first before trying Rust and Zig, so i don't have much to develop here except the choice of C++ instead of C 

The goal is to leverage C++'s compile time features like constexpr and templates, as well as some bonuses like ``using`` instead of the much worse typedef, or the ``enum class`` which is safer than a regular enum struct

